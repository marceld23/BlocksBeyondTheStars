[project]
name = "spacecraft-ai-backend"
version = "0.1.0"
description = "Optional AI mission backend for Spacecraft (separate Python service)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
]

[tool.uv]
package = false
